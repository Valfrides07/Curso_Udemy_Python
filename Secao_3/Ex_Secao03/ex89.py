# Exibir indices e a posição de cada nome, de uma lista utilizando laço For

lisst = ['Luisa', 'Maria', 'Claudio', 'Flavio']
indices = range(len(lisst)) #Len conta o número de itens de um objeto/Range gera uma sequência de números inteiros dentro de um intervalo especificado

for indice in indices:
    print(indice,':', lisst[indice]) #lisst[indice],está dizendo ao Python para acessar o elemento da lista lisst na posição indice
    if sdfaf: